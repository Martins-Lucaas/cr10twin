# ROS 2 Humble e cv_bridge são compilados contra a ABI do NumPy 1.x.
# NumPy 2.x quebra compatibilidade em tempo de execução → segfault.
numpy<2

# Só para scripts/gen_tcp_meshes_from_step.py, que regenera os meshes do TCP
# a partir de cad/step/*.step. Não é dependência de runtime dos nós ROS —
# os STL já vão versionados em meshes/.
#   python3 -m pip install cadquery-ocp
# cadquery-ocp

# Teleoperação da mão COVVI por câmera (palpation_gui → botão "CAM").
# OPCIONAL: o recurso faz import lazy e a GUI degrada para uma mensagem
# se faltar. NÃO fixado como dep dura porque `mediapipe` puxa
# `opencv-contrib-python` (colide com o opencv-python do sistema) e uma
# faixa de `protobuf` que pode brigar com a do ROS 2 Humble — instale
# num venv ou valide o conflito antes de promover.
#   python3 -m pip install "mediapipe>=0.10,<0.11"
# opencv-python já vem com o cv_bridge/ROS; mediapipe traz o seu.
# mediapipe>=0.10,<0.11
